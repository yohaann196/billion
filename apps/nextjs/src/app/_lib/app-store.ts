/**
 * Where "get the app" goes.
 *
 * Named once because it is now reached from several places — the Instagram bio
 * hop, the tracked-redirect table, and the call to action on every shared
 * record — and an App Store listing that moves would otherwise have to be
 * found in all of them.
 */
export const APP_STORE_URL =
  "https://apps.apple.com/us/app/billion-news/id6761675243";
